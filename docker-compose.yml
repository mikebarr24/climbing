version: '3.9'
services:
  api:
    build: ./api-climbing
    container_name: api
    ports:
      - 8080:8080
    volumes:
      - ./api-climbing:/app
  frontend:
    build: ./frontend-climb
    container_name: frontend
    ports:
      - 3000:3000
    volumes:
      - ./frontend-climb:/app
    depends_on:
      - api